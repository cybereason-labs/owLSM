# Test rule 27 - Out of range versions test

id: 27
description: "out of range versions"
action: "BLOCK_EVENT"
min_version: "99.99.99"
events:
    - CHMOD
detection:
    selection_target:
        target.file.path|endswith: "out_of_range_versions_test"
    condition: selection_target

