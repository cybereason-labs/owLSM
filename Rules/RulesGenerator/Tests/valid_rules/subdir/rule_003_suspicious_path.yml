id: 3
description: "Detect execution from suspicious paths"
action: "ALLOW_EVENT"
events:
    - CHMOD
detection:
    suspiciousPath:
        process.file.path|contains:
            - "chmodd"
            - "/var/tmp/"
            - "/dev/shm/"
    condition: suspiciousPath

