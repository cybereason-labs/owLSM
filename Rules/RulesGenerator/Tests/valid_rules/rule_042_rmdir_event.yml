id: 42
description: "Detect rmdir in suspicious locations"
action: "BLOCK_EVENT"
events:
    - RMDIR
detection:
    selection_suspicious_paths:
        target.file.path|startswith:
            - "/tmp/"
            - "/var/tmp/"
            - "/dev/shm/"
    condition: selection_suspicious_paths
