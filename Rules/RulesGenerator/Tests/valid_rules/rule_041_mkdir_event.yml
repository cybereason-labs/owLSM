id: 41
description: "Detect mkdir in suspicious locations"
action: "BLOCK_EVENT"
events:
    - MKDIR
detection:
    selection_suspicious_paths:
        target.file.path|startswith:
            - "/tmp/"
            - "/var/tmp/"
            - "/dev/shm/"
    condition: selection_suspicious_paths
