id: 4
description: "Detect script file execution"
action: "EXCLUDE_EVENT"
events:
    - CHMOD
detection:
    scriptFiles:
        - process.file.filename|endswith: ".sh"
        - process.file.filename|endswith: ".py"
        - process.file.filename|endswith: ".pl"
    condition: scriptFiles

