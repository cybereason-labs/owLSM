# This rule should fail validation - uses invalid field name
id: 100
description: "Invalid rule - bad field name"
action: "BLOCK_EVENT"
events:
    - CHMOD
detection:
    badSelection:
        InvalidField: "somevalue"
    condition: badSelection

