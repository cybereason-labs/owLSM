# This rule should fail validation - uses invalid action
id: 101
description: "Invalid rule - bad action"
action: "INVALID_ACTION"
events:
    - CHMOD
detection:
    selection:
        Filename: "test.exe"
    condition: selection

