# This rule should fail validation - missing condition
id: 102
description: "Invalid rule - no condition"
action: "BLOCK_EVENT"
events:
    - CHMOD
detection:
    selection:
        Filename: "test.exe"

