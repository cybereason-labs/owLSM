# This rule should fail validation - uses invalid modifier
id: 103
description: "Invalid rule - bad modifier"
action: "BLOCK_EVENT"
events:
    - CHMOD
detection:
    selection:
        process.cmd|regex: ".*evil.*"
    condition: selection

